# Railway 配置：https://docs.railway.com/reference/config-as-code
[build]
builder = "NIXPACKS"

[deploy]
# 直接跑 Node，少一层 npm；不设 healthcheckPath，避免 HTTP 探活误判导致一直 502
startCommand = "node src/app.js"
restartPolicyType = "ON_FAILURE"
