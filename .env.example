# Server（Railway 会注入 PORT，例如 8080；本地可写 3001）
PORT=3001
# Railway 重要：Dashboard → Networking → 公网域名后面的「Port」必须与上面进程监听的 PORT 完全一致。
# 常见错误：面板仍是 3001，但 Railway 给 PORT=8080 → 应用监听 8080 → 网关连 3001 → 502 connection refused
# 公网必须监听 0.0.0.0（默认已是 0.0.0.0，一般不用改）
HOST=0.0.0.0
# CORS：浏览器 Origin 必须出现在此列表中，否则预检 OPTIONS 会失败、控制台报 blocked by CORS
# 多个用英文逗号分隔，无空格；不要末尾斜杠；本地 + 生产一起写，例如：
# CLIENT_ORIGIN=http://localhost:5173,https://ai-turtle-soup-frontend-client.vercel.app
CLIENT_ORIGIN=http://localhost:5173

# DeepSeek API（必填，在平台「环境变量」里配置，不要提交到 Git）
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
